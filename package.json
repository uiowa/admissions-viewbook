{
  "name": "admissions-viewbook",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --assetsDir=assets --minify=esbuild",
    "build": "vite build --assetsDir=assets --minify=esbuild"
  },
  "dependencies": {
    "@uiowa/uids": "uiowa/uids#fff838b",
    "vue": "^3.0.0-rc.1"
  },
  "devDependencies": {
    "sass": "^1.26.10",
    "vite": "^1.0.0-rc.4"
  }
}
